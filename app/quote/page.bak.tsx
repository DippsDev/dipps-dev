import QuoteForm from '@/components/quoteForm';

export const metadata = {
    title: 'Get a Quote',
    description: "Contact to request a quote",
};

export default function QuotePageRoute() {
    return <QuoteForm />;
}
